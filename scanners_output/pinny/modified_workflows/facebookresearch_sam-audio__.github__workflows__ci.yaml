name: CI
on:
  push:
  pull_request:

env:
  CACHE_NUMBER: 0

jobs:
  build:
    runs-on: 32-core-ubuntu
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # actions/checkout@v4 | releases,v4.3.1

      - uses: mamba-org/setup-micromamba@422500192359a097648154e8db4e39bdb6c6eed7 # mamba-org/setup-micromamba@v1.8.1
        with:
          environment-name: sam-audio
          init-shell: bash
          create-args: >-
            python=3.11
            pip=24.2
            ruff==

      - uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830 # actions/cache@v4 | v4.3.0
        with:
          path: /home/runner/micromamba/envs/sam-audio
          key: ${{ hashFiles('pyproject.toml') }}-${{ env.CACHE_NUMBER }}
        id: cache

      - name: Update environment
        shell: bash -l {0}
        run: |
          pip install .
        if: steps.cache.outputs.cache-hit != 'true'

      - name: Check formatting
        shell: bash -l {0}
        run: |
          ruff format --check .

      - name: Lint
        shell: bash -l {0}
        run: |
          ruff check .
