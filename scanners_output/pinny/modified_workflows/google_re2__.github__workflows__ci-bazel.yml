name: CI (Bazel)
on:
  pull_request:
    branches: [main]
  push:
    branches: [main, rsc-testing]
permissions:
  contents: read
jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest, ubuntu-latest, windows-latest]
        # Keep in sync with python.yml.
        ver: ['3.10', '3.11', '3.12', '3.13', '3.14']
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # actions/checkout@v4.2.2
      - uses: bazel-contrib/setup-bazel@4fd964a13a440a8aeb0be47350db2fc640f19ca8 # bazel-contrib/setup-bazel@0.15.0
        with:
          bazelisk-version: '1.x'
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # actions/setup-python@v5.6.0 | releases,v5
        with:
          python-version: ${{ matrix.ver }}
      - name: Prepare Python ${{ matrix.ver }} environment
        run: |
          python -m pip install --upgrade pip
          python -m pip install --upgrade absl-py mypy
          python python/toolchains/generate.py
        shell: bash
      - run: .github/bazel.sh
        shell: bash
      # TODO(junyer): Run mypy as per https://github.com/google/re2/issues/496.
