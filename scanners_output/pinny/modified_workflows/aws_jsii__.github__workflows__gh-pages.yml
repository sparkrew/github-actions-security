# Workflow that publishes to the gh-pacges branch
name: GitHub Pages

on:
  merge_group: {}
  pull_request:
    branches: [main]
  push:
    branches: [main]
    paths: [gh-pages/**]
  workflow_dispatch:

jobs:
  build:
    name: build
    permissions:
      contents: read
    runs-on: ubuntu-latest
    steps:
      - name: Check out
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # actions/checkout@v6 | v6.0.2
      - name: Set up Python
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # actions/setup-python@v6 | main,v6.2.0
        with:
          cache: 'pip'
          cache-dependency-path: 'gh-pages/requirements-dev.txt'
          python-version: '3.9'
      - name: Install Dependencies
        run: |-
          python3 -m venv ${{ runner.temp }}/venv
          source ${{ runner.temp }}/venv/bin/activate
          python3 -m pip install -r requirements-dev.txt
        working-directory: gh-pages
      - name: Build DocSite
        run: |-
          mkdir -p ${{ runner.temp }}/site
          source ${{ runner.temp }}/venv/bin/activate
          mkdocs build                                                          \
            --strict                                                            \
            --site-dir ${{ runner.temp }}/site
        working-directory: gh-pages
      - name: Upload Artifact
        uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # actions/upload-artifact@v7 | main,v7.0.0
        with:
          name: doc-site
          path: ${{ runner.temp }}/site/

  publish:
    name: Publish
    needs: build
    if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
    permissions:
      contents: write
    runs-on: ubuntu-latest
    steps:
      - name: Check out
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # actions/checkout@v6 | v6.0.2
        with:
          ref: gh-pages
          token: ${{ secrets.PROJEN_GITHUB_TOKEN }}
      - name: Download Artifact
        uses: actions/download-artifact@70fc10c6e5e1ce46ad2ea6f2b72d43f7d47b13c3 # actions/download-artifact@v8 | main,209,v8.0.0
        with:
          name: doc-site
          path: ${{ runner.temp }}/site
      - name: Configure Git
        run: |-
          git config user.name "AWS CDK Automation"
          git config user.email "aws-cdk+automation@amazon.com"
      - name: Prepare Commit
        run: |-
          rsync --archive --checksum --delete --inplace --recursive --verbose   \
            --exclude=.git --exclude=.nojekyll --exclude=dev                    \
            ${{ runner.temp }}/site/ ./
          git add .
          git diff --cached --exit-code >/dev/null || (
            git commit -am 'docs: publish from ${{ github.sha }}'
          )
      - name: Push
        run: |-
          git push origin gh-pages:gh-pages
