# Copyright (c) Meta Platforms, Inc. and affiliates.

name: OpenZL Demo Wheels

on:
  workflow_dispatch:
  pull_request:
    branches:
      - openzl-demo
  push:
    branches:
     - openzl-demo

jobs:
  build_sdist:
    name: Build SDist
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # actions/checkout@v4 | releases,v4.3.1
      with:
        submodules: true

    - name: Build SDist
      run: pipx run build --sdist contrib/openzl-demo

    - name: Check metadata
      run: pipx run twine check contrib/openzl-demo/dist/*

    - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # actions/upload-artifact@v4 | v4.6.2
      with:
        name: dist-sdist
        path: contrib/openzl-demo/dist/*.tar.gz


  build_wheels:
    name: Wheels on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        # Once MSVC is supported we can build Windows packages as well by adding windows-latest
        # Once we're public we can build on ubuntu-24.04-arm & windows-11-arm.
        # They are not supported for private repos.
        # TODO: Remove filesystem for macos-13
        os: [ubuntu-latest, macos-latest]

    steps:
    - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # actions/checkout@v4 | releases,v4.3.1
      with:
        submodules: true

    - uses: pypa/cibuildwheel@5f22145df44122af0f5a201f93cf0207171beca7 # pypa/cibuildwheel@v3.0.0
      with:
        package-dir: contrib/openzl-demo

    - name: Verify clean directory
      run: git diff --exit-code
      shell: bash

    - name: Upload wheels
      uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # actions/upload-artifact@v4 | v4.6.2
      with:
        path: wheelhouse/*.whl
        name: dist-${{ matrix.os }}
