name: TagIt

on:
  schedule:
    - cron: '0 0 * * 1' # Every Monday

jobs:
  tag:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@f43a0e5ff2bd294095638e18286ca9a3d1956744 # actions/checkout@v3 | releases,v3.6.0

      - name: Get tag for the current date
        id: tag_info
        run: echo "TAG_NAME=$(date +'v%Y.%m.%d')" >> $GITHUB_OUTPUT

      - name: Tag the latest commit
        uses: weareyipyip/walking-tag-action@f9c82a63cad72753310f9342e63e8e29f8c259e5 # weareyipyip/walking-tag-action@v2 | master,v2.0.1
        with:
          tag-name: ${{ steps.tag_info.outputs.TAG_NAME }}
          tag-message: ""
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
