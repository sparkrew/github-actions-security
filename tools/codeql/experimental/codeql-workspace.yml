# This CodeQL workspace file is designed to be placed in
# `experimental/codeql-workspace.yml` within a CodeQL CLI distribution.
# The packs provided by it will only be discoverable if `CODEQL_ENABLE_EXPERIMENTAL_FEATURES=true`
provide:
  - "*/codeql-extractor.yml"
