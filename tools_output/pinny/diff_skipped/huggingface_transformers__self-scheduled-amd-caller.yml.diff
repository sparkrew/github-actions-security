--- huggingface_transformers__self-scheduled-amd-caller.yml (original)
+++ huggingface_transformers__self-scheduled-amd-caller.yml (modified)
@@ -1,14 +1,12 @@
 name: Self-hosted runner (AMD scheduled CI caller)
-
 on:
-  schedule:
-    - cron: "17 2 * * *"
-
+schedule:
+- cron: "17 2 * * *"
 jobs:
-  run_scheduled_amd_ci:
-    name: Trigger Scheduled AMD CI
-    runs-on: ubuntu-22.04
-    if: ${{ always() }}
-    steps:
-      - name: Trigger scheduled AMD CI via workflow_run
-        run: echo "Trigger scheduled AMD CI via workflow_run"
+run_scheduled_amd_ci:
+name: Trigger Scheduled AMD CI
+runs-on: ubuntu-22.04
+if: ${{ always() }}
+steps:
+- name: Trigger scheduled AMD CI via workflow_run
+run: echo "Trigger scheduled AMD CI via workflow_run"